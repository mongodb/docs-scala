define: prefix docs/languages/scala/scala-driver
define: base https://www.mongodb.com/${prefix}
define: versions v4.x main

symlink: current -> master

raw: ${prefix}/ -> ${base}/current/
raw: ${prefix}/stable -> ${base}/current/
